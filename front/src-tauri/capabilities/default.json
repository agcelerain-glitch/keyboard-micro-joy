{
  "identifier": "default",
  "description": "Default capability — window control + opener",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "core:window:allow-set-position",
    "core:window:allow-start-dragging",
    "opener:default"
  ]
}
